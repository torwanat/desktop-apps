# Wyswig

Simple Tkinter WYSIWYG-style editor for basic text formatting and file operations.

Features
- Open/save files
- Basic text formatting (bold, italic, underline)

Quick start
1. Run `main.py` in the `Wyswig` folder with Python 3.7+.

See the code for available formatting commands and keyboard shortcuts.
